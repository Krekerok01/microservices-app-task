server:
  port: 0

spring:
  application:
    name: news-service

  config:
    import: optional:file:./news/.env[.properties]


eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
  client:
    service-url:
      defaultZone: ${EUREKA_URL}